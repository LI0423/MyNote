<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (C) 2022 Baidu, Inc. All Rights Reserved.
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.video.manager.mapper.SspMediationMapper">

    <select id="getAidList" resultType = "com.video.manager.domain.dto.MediationDTO">
        select a.id, a.base_price as price, a.slot_name as name
        from ssp_mediation a
        left join ssp_app_placement b
        on a.placement_id=b.id
        left join ssp_app_base c
        on b.app_id=c.id
        where c.pkg=#{pkg}
        order by base_price desc;
    </select>
</mapper>